/*

#include "cprocessing.hpp"

using namespace cprocessing;

namespace cprocessing {
    struct Style {
        int rectMode;
        int ellipseMode;
        int imageMode;
        int colorMode;

        int ellipseDetail;
        int sphereDetail;

        int strokeWeight;

        color stroke;
        color fill;

        Style ( ) {
            if(styleList.size() > 1) {
                rectMode;
                ellipseMode;
                imageMode;
                colorMode;
                ellipseDetail;
                sphereDetail;
                strokeWeight;
                stroke;
                fill;
            }
        }
    };
}

*/