#include "cprocessing.hpp"

using namespace cprocessing;
/*
namespace cprocessing {
	PThread::PThread() {
		self = make_thread_wrapper(std::bind(&callable_method, 44));
		self.join();
	}

	void PThread::callable_method(int x) {
		println(x);
	}
}
*/
